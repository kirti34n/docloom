# Shell scripts must stay LF so hooks run on the Linux (web) container,
# even when checked out or re-added on Windows (core.autocrlf=true).
*.sh text eol=lf

# Windows launchers must stay CRLF so cmd.exe / PowerShell parse them cleanly.
*.ps1 text eol=crlf
*.cmd text eol=crlf

# maintainer: krt
